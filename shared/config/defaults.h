#pragma once

// Control defaults
static const float DEFAULT_SETPOINT_C = 25.0f;
static const float DEFAULT_HYSTERESIS_C = 0.5f;

// Relay channel selection (UNO side)
static const int RELAY_PIN = 4; // R1 = D4
